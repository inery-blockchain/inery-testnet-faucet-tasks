### install ###
1. install nodejs
```shell
curl -fsSL https://deb.nodesource.com/setup_19.x | sudo -E bash - &&\
sudo apt-get install  nodejs -y
2. install npm
sudo apt install npm
### RUN ###
1. cd ./inery-testnet-faucet-tasks/awan
2. npm install
3. cp .env-example .env
4. edit .env
5. npm install dotenv  ///NOTE : if "dotenv" is not installed
6. npm run awan
7. Done
